spring.application.name=pactos

# URL de conexão com o banco de dados que acabamos de criar
spring.datasource.url=jdbc:mysql://localhost:3306/pactos_db?serverTimezone=UTC
# Seu usuário do MySQL (o padrão é root)
spring.datasource.username=root
# Sua senha do MySQL (Troque pela sua senha de verdade)
spring.datasource.password=050103021106Ae!@

# Configurações mágicas do Hibernate (JPA)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
